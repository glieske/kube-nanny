apiVersion: v1
kind: Service
metadata:
  name: {{ include "kube-nanny.fullname" . }}
  labels:
    {{- include "kube-nanny.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kube-nanny.selectorLabels" . | nindent 4 }}
